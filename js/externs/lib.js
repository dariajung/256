/** 
 * @fileoverview Definitions for lib.js
 * @externs
 */

/**
 * @constructor
 */
function dash() {};

/**
 * @param {Function} fxn
 * @param {Array<number>} obj
 * @return {?Array<number>}
 */
dash.prototype.map = function(fxn, obj){};

/**
 * @param {Function} fxn
 * @param {Array<number>} obj
 * @return {?Array<number>}
 */
dash.map = function(fxn, obj){};

/**
 * @param {Array} obj
 * @return {?Array}
 */
dash.prototype.flip = function(obj){};

/**
 * @param {Array} obj
 * @return {?Array}
 */
dash.flip = function(obj){};

/**
 * @param {Function} cond
 * @param {Array} obj
 * @return {?Array}
 */
dash.prototype.filter = function(cond, obj){};

/**
 * @param {Function} cond
 * @param {Array} obj
 * @return {?Array}
 */
dash.filter = function(cond, obj){};